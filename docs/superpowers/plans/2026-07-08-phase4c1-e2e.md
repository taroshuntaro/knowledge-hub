# Phase 4c-1: E2E スイート Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Playwright E2E スイート（8 spec + ローカル限定 SSO）を専用隔離スタック上に構築し、CI に別ジョブで組み込む。

**Architecture:** 新 workspace パッケージ `tests/e2e`（`@playwright/test`）。web は**本番ビルドを `vite preview`（:54173）**で配信し `/api` を E2E 用 API サーバー（:53000）へ proxy。バックエンドは compose プロジェクト `khub-e2e`（db :55432 / mailpit :51025+:58025 / minio :59000）で dev と完全分離、毎回 migrate+seed。ユーザー準備は Playwright の **setup プロジェクト**（admin ログイン → 招待 → Mailpit REST → 受諾 → member 作成、両者の storageState 保存）で行い、spec ファイルの実行順に依存させない。

**Tech Stack:** @playwright/test（chromium）、docker compose オーバーレイ（`!override`、Compose v2.24.4+）、Mailpit REST API、tsx --env-file、GitHub Actions。

## Global Constraints

- E2E パッケージに **`test` という名前の script を作らない**（`pnpm -r test` = verify に E2E が混入するため）。実行 script 名は `e2e`。
- `pnpm run verify` の内容・所要時間を変えない（typecheck に tests/e2e が加わるのは可）。
- アプリ挙動の変更禁止。許可される変更は: `apps/web/vite.config.ts` の proxy 先 env 化（デフォルト `http://localhost:3000` 不変）／`apps/web/package.json`・`apps/server/package.json` への script 追加／`docker/keycloak/realm.json` の redirectUris 追記／必要最小限の `data-testid`（今回の計画では不要の見込み）。
- ポート割当（固定）: API **53000** / web preview **54173** / db **55432** / mailpit SMTP **51025**・UI+REST **58025** / minio **59000**・console **59001**。dev（3000/5173/5432/1025/8025/9000）と非衝突。
- `.env.e2e` はコミットする（隔離スタックのローカルポートと compose 記載済み dev 認証情報のみ。`.gitignore` は `.env` 完全一致なので対象外）。
- テストは任意 sleep 禁止（Playwright の auto-wait / `expect` ポーリングを使う）。workers=1・直列。retries は CI のみ 1。
- コミットは英語 Conventional Commits。
- シード admin = admin@example.com / change-me-please-12（dev 専用・リポジトリ既出）。E2E member = e2e-member@example.com / e2e-member-pass-12（12 文字以上必須）。

## File Structure

- `docker-compose.e2e.yml` — ポートオーバーレイのみ（新規）
- `apps/server/.env.e2e` — E2E 用 env（新規・コミット）
- `apps/server/package.json` — `start:e2e` / `db:migrate:e2e` / `seed:admin:e2e` 追加
- `apps/web/vite.config.ts` — proxy 先を `process.env.API_PROXY_TARGET ?? 'http://localhost:3000'` に
- `pnpm-workspace.yaml` — `tests/*` 追加
- `package.json`（ルート） — `e2e:up` / `e2e` / `e2e:down` 追加
- `docker/keycloak/realm.json` — redirectUris に :54173 コールバック追記
- `.gitignore` — E2E 生成物（test-results / playwright-report / .auth）
- `tests/e2e/package.json`, `tsconfig.json`, `playwright.config.ts`
- `tests/e2e/helpers/{data,mailpit,api}.ts` — 一意名・PNG フィクスチャ／Mailpit REST／API 直叩き（origin ヘッダ付き）
- `tests/e2e/setup/auth.setup.ts` — storageState 準備（招待フロー検証を兼ねる）
- `tests/e2e/specs/{auth,mobile,sso,article,hero,search,engagement,notification}.spec.ts`
- `.github/workflows/ci.yml` — 並列 `e2e` ジョブ

## 確認済みセレクタ一覧（実コードから採取。変更しないこと）

| 画面 | セレクタ |
|---|---|
| ログイン | `getByLabel('メールアドレス')` / `getByLabel('パスワード')` / `getByRole('button', { name: 'ログイン' })` / SSO は `getByRole('link', { name: 'SSO でログイン' })` |
| サイドバー | アカウントメニュートリガー = 表示名テキストのボタン（admin は `管理者`）／`ログアウト` menuitem |
| モバイル | ハンバーガー = `getByRole('button', { name: 'メニューを開く' })`、ドロワー = `getByRole('dialog')` |
| 招待 | `getByLabel('招待するメールアドレス')` / `getByRole('button', { name: '招待を送る' })`；受諾 = `getByLabel('表示名')` / `getByLabel('パスワード（12文字以上）')` / `getByRole('button', { name: '登録する' })` |
| エディタ（S5） | タイトル = `getByPlaceholder('タイトルを入力')`；本文 = `.ProseMirror`；保存 = テキスト `保存しました` or `保存済み`；公開パネル = `getByRole('dialog')` 内 `getByLabel(/カテゴリ/)`（select）+ `getByRole('button', { name: '公開する' })`；ヒーロー input = `getByLabel('ヒーロー画像を選択')`、プレビュー = `getByRole('img', { name: 'ヒーロー画像' })` |
| カテゴリ管理 | `getByLabel('名称')` / `getByRole('button', { name: '追加' })` |
| 検索 | `getByLabel('キーワード')` / `getByRole('button', { name: '検索' })` |
| コメント | 入力 = `getByLabel('コメント')`；投稿 = `getByRole('button', { name: 'コメントする' })`；返信トリガー = `getByRole('button', { name: '返信' })`；返信投稿 = `getByRole('button', { name: '返信する' })` |
| リアクション | `getByRole('group', { name: 'リアクション' })` 内の絵文字ボタン（`aria-pressed` あり、テキストに件数） |
| ブックマーク | `getByRole('button', { name: 'ブックマーク' })` → 押下後 `ブックマーク済み` |
| 通知 | ベル = `getByRole('button', { name: '通知' })`、未読バッジ = `aria-label="未読 N 件"` |

---

### Task 1: 隔離スタック + 配線（インフラ）

**Files:**
- Create: `docker-compose.e2e.yml`
- Create: `apps/server/.env.e2e`
- Modify: `apps/server/package.json`（scripts に 3 行）
- Modify: `apps/web/vite.config.ts:9`
- Modify: `pnpm-workspace.yaml`
- Modify: `package.json`（ルート scripts）
- Modify: `docker/keycloak/realm.json:12`
- Modify: `.gitignore`

**Interfaces:**
- Produces: `pnpm run e2e:up`（スタック起動+migrate+seed）／`pnpm run e2e:down`／`pnpm --filter @knowledge-hub/server start:e2e`（API :53000）／`API_PROXY_TARGET` env（vite preview の /api proxy 先）。Task 2 以降はこれらを前提にする。

- [ ] **Step 1: docker-compose.e2e.yml を作成**

```yaml
# E2E 用ポートオーバーレイ。dev スタックと同居できるよう公開ポートだけ差し替える。
# 起動例:
#   docker compose -p khub-e2e -f docker-compose.yml -f docker-compose.e2e.yml up -d --wait db mailpit minio minio-setup
# ports の置換に !override を使うため Docker Compose v2.24.4+ が必要。
services:
  db:
    ports: !override
      - "55432:5432"
  mailpit:
    ports: !override
      - "51025:1025"
      - "58025:8025"
  minio:
    ports: !override
      - "59000:9000"
      - "59001:9001"
```

- [ ] **Step 2: apps/server/.env.e2e を作成**

```bash
# E2E 専用サーバー設定（コミット対象）。
# 値は docker-compose.e2e.yml の隔離スタック（ローカルポート）と
# リポジトリ既出の dev 認証情報のみ。秘密情報を追加しないこと。
NODE_ENV=development
PORT=53000
DATABASE_URL=postgres://khub:khub@localhost:55432/khub
APP_URL=http://localhost:54173
SMTP_HOST=localhost
SMTP_PORT=51025
SMTP_FROM=knowledge-hub@example.com
S3_ENDPOINT=http://localhost:59000
S3_REGION=us-east-1
S3_BUCKET=knowledge-hub
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
S3_FORCE_PATH_STYLE=true
# seed:admin:e2e 用
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-please-12
# SSO spec（ローカル限定）用。Keycloak（dev compose --profile idp, :8080）不在でも
# discovery は遅延実行なのでサーバー起動・他specに影響しない（ログイン画面に SSO ボタンが出るだけ）。
OIDC_ISSUER=http://localhost:8080/realms/knowledge-hub
OIDC_CLIENT_ID=knowledge-hub
OIDC_CLIENT_SECRET=dev-keycloak-secret
```

- [ ] **Step 3: apps/server/package.json の scripts に追加**

既存 scripts ブロックに 3 行追加（他は不変）:

```json
    "start:e2e": "tsx --env-file=.env.e2e src/index.ts",
    "db:migrate:e2e": "DATABASE_URL=postgres://khub:khub@localhost:55432/khub drizzle-kit migrate",
    "seed:admin:e2e": "tsx --env-file=.env.e2e src/scripts/seed-admin.ts"
```

（drizzle.config.ts は `dotenv/config` だが、明示 env は dotenv に上書きされないため `DATABASE_URL=... drizzle-kit migrate` で隔離 DB に向く。）

- [ ] **Step 4: apps/web/vite.config.ts の proxy 先を env 化**

9 行目を次に変更（デフォルト値により dev 挙動は完全不変）:

```ts
  server: { proxy: { '/api': process.env.API_PROXY_TARGET ?? 'http://localhost:3000' } },
```

（`vite preview` の proxy は `preview.proxy` が未指定なら `server.proxy` を継承する。）

- [ ] **Step 5: pnpm-workspace.yaml に tests/* を追加**

```yaml
packages:
  - "apps/*"
  - "packages/*"
  - "tests/*"
```

- [ ] **Step 6: ルート package.json に scripts 追加**

```json
    "e2e:up": "docker compose -p khub-e2e -f docker-compose.yml -f docker-compose.e2e.yml up -d --wait db mailpit minio minio-setup && pnpm --filter @knowledge-hub/server db:migrate:e2e && pnpm --filter @knowledge-hub/server seed:admin:e2e",
    "e2e": "pnpm --filter @knowledge-hub/web build && pnpm --filter @knowledge-hub/e2e e2e",
    "e2e:down": "docker compose -p khub-e2e -f docker-compose.yml -f docker-compose.e2e.yml down -v"
```

- [ ] **Step 7: realm.json の redirectUris に E2E コールバックを追記**

12 行目を:

```json
      "redirectUris": ["http://localhost:5173/api/auth/oidc/callback", "http://localhost:54173/api/auth/oidc/callback"]
```

- [ ] **Step 8: .gitignore に E2E 生成物を追加**

```
tests/e2e/test-results/
tests/e2e/playwright-report/
tests/e2e/.auth/
```

- [ ] **Step 9: スタック起動スモーク**

Run（順に）:
1. `pnpm run e2e:up` → compose 3+1 サービス起動、migrate 適用ログ、`admin を作成しました` 出力
2. `pnpm --filter @knowledge-hub/server start:e2e` をバックグラウンド起動 → `curl -s -o /dev/null -w "%{http_code}" http://localhost:53000/healthz` = 200
3. `pnpm --filter @knowledge-hub/web build` → `API_PROXY_TARGET=http://localhost:53000 pnpm --dir apps/web exec vite preview --port 54173 --strictPort` をバックグラウンド起動 → `curl -s http://localhost:54173/api/auth/methods` が `{"password":true,"oidc":true}` を返す（proxy 経由で API に到達）
4. スモーク後、手動起動した server/preview は停止（スタックは起動したままで良い）

Expected: すべて成功。dev スタック（:5432 等）と衝突しない。

- [ ] **Step 10: Commit**

```bash
git add docker-compose.e2e.yml apps/server/.env.e2e apps/server/package.json apps/web/vite.config.ts pnpm-workspace.yaml package.json docker/keycloak/realm.json .gitignore
git commit -m "feat(e2e): add isolated e2e stack and wiring"
```

---

### Task 2: Playwright パッケージ + setup プロジェクト

**Files:**
- Create: `tests/e2e/package.json`
- Create: `tests/e2e/tsconfig.json`
- Create: `tests/e2e/playwright.config.ts`
- Create: `tests/e2e/helpers/data.ts`
- Create: `tests/e2e/helpers/mailpit.ts`
- Create: `tests/e2e/helpers/api.ts`
- Create: `tests/e2e/setup/auth.setup.ts`

**Interfaces:**
- Consumes: Task 1 の `start:e2e` / `API_PROXY_TARGET` / 隔離スタック。
- Produces: `.auth/admin.json`・`.auth/member.json`（storageState。specs のデフォルトは admin、member は `browser.newContext({ storageState: '.auth/member.json' })` で利用）。helpers: `unique(prefix): string`／`ADMIN`・`MEMBER` 定数／`pngPixel: Buffer`／`clearMailbox()`／`latestMessageText(to): Promise<string>`／`createCategoryViaApi(page, name): Promise<string>`／`createPublishedArticleViaApi(page, {title, bodyMd, categoryId}): Promise<string>`。

- [ ] **Step 1: package.json / tsconfig.json**

`tests/e2e/package.json`（**`test` script は作らない** — Global Constraints 参照）:

```json
{
  "name": "@knowledge-hub/e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2"
  }
}
```

`tests/e2e/tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["**/*.ts"]
}
```

`pnpm install` を実行し lockfile を更新。ブラウザ未導入環境では `pnpm --filter @knowledge-hub/e2e exec playwright install chromium` （このマシンは chromium-1223 キャッシュ済みのため通常不要）。

- [ ] **Step 2: playwright.config.ts**

```ts
import { defineConfig, devices } from '@playwright/test';

const WEB_URL = 'http://localhost:54173';
const API_URL = 'http://localhost:53000';

export default defineConfig({
  testDir: '.',
  workers: 1,
  fullyParallel: false,
  retries: process.env.CI ? 1 : 0,
  reporter: process.env.CI ? [['list'], ['html', { open: 'never' }]] : 'list',
  use: {
    baseURL: WEB_URL,
    trace: 'retain-on-failure',
    screenshot: 'only-on-failure',
  },
  projects: [
    { name: 'setup', testMatch: /setup\/.*\.setup\.ts/, use: { ...devices['Desktop Chrome'] } },
    {
      name: 'chromium',
      testMatch: /specs\/.*\.spec\.ts/,
      testIgnore: /specs\/sso\.spec\.ts/,
      dependencies: ['setup'],
      use: { ...devices['Desktop Chrome'], storageState: '.auth/admin.json' },
    },
    // SSO はローカル限定（Keycloak 未起動なら spec 内で skip）。storageState なしの素の状態で走る。
    { name: 'sso', testMatch: /specs\/sso\.spec\.ts/, use: { ...devices['Desktop Chrome'] } },
  ],
  webServer: [
    {
      command: 'pnpm --dir ../../apps/server start:e2e',
      url: `${API_URL}/healthz`,
      reuseExistingServer: !process.env.CI,
      timeout: 30_000,
    },
    {
      command: 'pnpm --dir ../../apps/web exec vite preview --port 54173 --strictPort',
      url: WEB_URL,
      reuseExistingServer: !process.env.CI,
      timeout: 30_000,
      env: { API_PROXY_TARGET: API_URL },
    },
  ],
});
```

注意: `pnpm --filter @knowledge-hub/e2e e2e` は package dir を cwd に実行されるため、`.auth/...` や `--dir ../../apps/...` の相対パスは tests/e2e 起点で正しい。web の本番ビルドはルート `e2e` script（Task 1）が事前実行する。

- [ ] **Step 3: helpers/data.ts**

```ts
export function unique(prefix: string): string {
  return `${prefix}-${Date.now()}-${Math.floor(Math.random() * 1000)}`;
}

export const ADMIN = { email: 'admin@example.com', password: 'change-me-please-12', name: '管理者' };
export const MEMBER = { email: 'e2e-member@example.com', password: 'e2e-member-pass-12', name: 'E2E メンバー' };

// 1x1 の赤 PNG（アップロードの magic-byte 検証を通る実 PNG）
export const pngPixel = Buffer.from(
  'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==',
  'base64',
);
```

- [ ] **Step 4: helpers/mailpit.ts**

```ts
const MAILPIT = 'http://localhost:58025';

export async function clearMailbox(): Promise<void> {
  await fetch(`${MAILPIT}/api/v1/messages`, { method: 'DELETE' });
}

type MessageSummary = { ID: string; To: { Address: string }[] };

/** 宛先に一致する最新メールの本文テキストを返す（受信までポーリング） */
export async function latestMessageText(to: string, timeoutMs = 10_000): Promise<string> {
  const deadline = Date.now() + timeoutMs;
  while (Date.now() < deadline) {
    const res = await fetch(`${MAILPIT}/api/v1/messages`);
    const body = (await res.json()) as { messages: MessageSummary[] };
    const hit = body.messages.find((m) => m.To.some((t) => t.Address === to));
    if (hit) {
      const detail = await fetch(`${MAILPIT}/api/v1/message/${hit.ID}`);
      const msg = (await detail.json()) as { Text: string };
      return msg.Text;
    }
    await new Promise((r) => setTimeout(r, 250));
  }
  throw new Error(`mail to ${to} not received within ${timeoutMs}ms`);
}
```

- [ ] **Step 5: helpers/api.ts**

サーバーの origin チェック（APP_URL 一致）を通すため、mutation には必ず origin ヘッダを付ける。

```ts
import { expect, type Page } from '@playwright/test';

const ORIGIN = { origin: 'http://localhost:54173' };

/** admin セッション（page の cookie）でカテゴリを API 作成し id を返す */
export async function createCategoryViaApi(page: Page, name: string): Promise<string> {
  const res = await page.request.post('/api/categories', { data: { name }, headers: ORIGIN });
  expect(res.ok(), `createCategory: ${res.status()}`).toBeTruthy();
  const body = (await res.json()) as { id: string };
  return body.id;
}

/** 記事を作成して公開し、記事 id を返す（page の cookie のユーザーが著者になる） */
export async function createPublishedArticleViaApi(
  page: Page,
  input: { title: string; bodyMd: string; categoryId: string },
): Promise<string> {
  const created = await page.request.post('/api/articles', {
    data: { title: input.title, bodyMd: input.bodyMd, categoryId: input.categoryId, heroImageUploadId: null, tags: [] },
    headers: ORIGIN,
  });
  expect(created.ok(), `createArticle: ${created.status()}`).toBeTruthy();
  const { id } = (await created.json()) as { id: string };
  const published = await page.request.post(`/api/articles/${id}/publish`, { headers: ORIGIN });
  expect(published.ok(), `publish: ${published.status()}`).toBeTruthy();
  return id;
}
```

- [ ] **Step 6: setup/auth.setup.ts**

```ts
import { expect, test as setup } from '@playwright/test';
import { ADMIN, MEMBER } from '../helpers/data';
import { clearMailbox, latestMessageText } from '../helpers/mailpit';

setup('admin ログインと member 招待（storageState 準備）', async ({ page, browser }) => {
  // --- admin: パスワードログイン → storageState 保存 ---
  await page.goto('/login');
  await page.getByLabel('メールアドレス').fill(ADMIN.email);
  await page.getByLabel('パスワード').fill(ADMIN.password);
  await page.getByRole('button', { name: 'ログイン' }).click();
  await expect(page).toHaveURL('/');
  await page.context().storageState({ path: '.auth/admin.json' });

  // --- member: 既存ならログイン、なければ招待フローで作成（招待フローの検証を兼ねる） ---
  const memberContext = await browser.newContext({ baseURL: 'http://localhost:54173' });
  const probe = await memberContext.request.post('/api/auth/login', {
    data: { email: MEMBER.email, password: MEMBER.password },
    headers: { origin: 'http://localhost:54173' },
  });
  if (!probe.ok()) {
    await clearMailbox();
    // admin が招待を送る
    await page.goto('/admin');
    await page.getByLabel('招待するメールアドレス').fill(MEMBER.email);
    await page.getByRole('button', { name: '招待を送る' }).click();
    // Mailpit REST で招待リンクを取得
    const text = await latestMessageText(MEMBER.email);
    const inviteUrl = text.match(/http:\/\/localhost:54173\/invite\/[\w.-]+/)?.[0];
    expect(inviteUrl, `招待メールにリンクが見つからない: ${text}`).toBeTruthy();
    // member 側コンテキストで受諾 → 自動ログイン
    const memberPage = await memberContext.newPage();
    await memberPage.goto(inviteUrl!);
    await memberPage.getByLabel('表示名').fill(MEMBER.name);
    await memberPage.getByLabel('パスワード（12文字以上）').fill(MEMBER.password);
    await memberPage.getByRole('button', { name: '登録する' }).click();
    await expect(memberPage).toHaveURL('/');
    await memberPage.close();
  }
  await memberContext.storageState({ path: '.auth/member.json' });
  await memberContext.close();
});
```

- [ ] **Step 7: setup 単体で実行して確認**

Run: `pnpm run e2e -- --project=setup`（ルートから。web build → webServer 起動 → setup 実行）
Expected: PASS。`tests/e2e/.auth/admin.json`・`member.json` が生成される。Mailpit UI（http://localhost:58025）に招待メールが残る。

- [ ] **Step 8: typecheck**

Run: `pnpm --filter @knowledge-hub/e2e typecheck`
Expected: エラーなし。

- [ ] **Step 9: Commit**

```bash
git add tests/e2e pnpm-lock.yaml
git commit -m "feat(e2e): add playwright package with auth setup project"
```

---

### Task 3: auth / mobile / sso spec

**Files:**
- Create: `tests/e2e/specs/auth.spec.ts`
- Create: `tests/e2e/specs/mobile.spec.ts`
- Create: `tests/e2e/specs/sso.spec.ts`

**Interfaces:**
- Consumes: Task 2 の helpers（`ADMIN`）と storageState（mobile は admin デフォルト）。sso は素の状態（sso プロジェクト）。

- [ ] **Step 1: auth.spec.ts**

```ts
import { expect, test } from '@playwright/test';
import { ADMIN } from '../helpers/data';

// ログイン UI 自体を検証するため、保存済みセッションは使わない
test.use({ storageState: { cookies: [], origins: [] } });

test('誤ったパスワードで role=alert のエラーが出て /login に留まる', async ({ page }) => {
  await page.goto('/login');
  await page.getByLabel('メールアドレス').fill(ADMIN.email);
  await page.getByLabel('パスワード').fill('wrong-password-123');
  await page.getByRole('button', { name: 'ログイン' }).click();
  await expect(page.getByRole('alert')).toBeVisible();
  await expect(page).toHaveURL(/\/login/);
});

test('ログイン成功でフィードへ、ログアウトで /login へ戻る', async ({ page }) => {
  await page.goto('/login');
  await page.getByLabel('メールアドレス').fill(ADMIN.email);
  await page.getByLabel('パスワード').fill(ADMIN.password);
  await page.getByRole('button', { name: 'ログイン' }).click();
  await expect(page).toHaveURL('/');
  // サイドバー下部のアカウントメニュー（トリガーは表示名）
  await page.getByRole('button', { name: ADMIN.name }).click();
  await page.getByRole('menuitem', { name: 'ログアウト' }).click();
  await expect(page).toHaveURL(/\/login/);
});
```

- [ ] **Step 2: mobile.spec.ts**

```ts
import { expect, test } from '@playwright/test';

test.use({ viewport: { width: 375, height: 812 } });

test('モバイル: ドロワーを開いてナビ、遷移で自動クローズ、横スクロールなし', async ({ page }) => {
  await page.goto('/');
  await page.getByRole('button', { name: 'メニューを開く' }).click();
  const drawer = page.getByRole('dialog');
  await expect(drawer).toBeVisible();
  await drawer.getByRole('link', { name: '検索' }).click();
  await expect(page).toHaveURL(/\/search/);
  await expect(page.getByRole('dialog')).toBeHidden();
  const overflow = await page.evaluate(() => document.documentElement.scrollWidth - window.innerWidth);
  expect(overflow).toBeLessThanOrEqual(0);
});
```

- [ ] **Step 3: sso.spec.ts**

```ts
import { expect, test } from '@playwright/test';

const KC_DISCOVERY = 'http://localhost:8080/realms/knowledge-hub/.well-known/openid-configuration';

// ローカル限定: dev Keycloak（docker compose --profile idp up -d）起動時のみ実行。
test('SSO ログイン: Keycloak → JIT → member 到達', async ({ page }) => {
  const alive = await fetch(KC_DISCOVERY).then((r) => r.ok).catch(() => false);
  test.skip(!alive, 'Keycloak（--profile idp, :8080）未起動のためスキップ');

  await page.goto('/login');
  await page.getByRole('link', { name: 'SSO でログイン' }).click();
  // Keycloak のログイン画面（標準テーマの id）
  await page.locator('#username').fill('sso-taro');
  await page.locator('#password').fill('sso-dev-password');
  await page.locator('#kc-login').click();
  // コールバック → JIT → フィードへ
  await expect(page).toHaveURL('http://localhost:54173/');
  await expect(page.getByRole('button', { name: 'Taro SSO' })).toBeVisible(); // サイドバーの表示名
});
```

- [ ] **Step 4: 実行して確認**

Run: `pnpm run e2e -- --project=chromium --grep-invert "article|hero|search|engagement|notification"` は複雑なので、単純にファイル指定で:
`pnpm run e2e -- specs/auth.spec.ts specs/mobile.spec.ts` と `pnpm run e2e -- --project=sso`
Expected: auth 2 件 + mobile 1 件 PASS。sso は Keycloak 未起動なら skipped 1（起動していれば PASS）。

- [ ] **Step 5: Commit**

```bash
git add tests/e2e/specs
git commit -m "feat(e2e): add auth, mobile drawer, and optional sso specs"
```

---

### Task 4: article / hero spec（S5 エディタの通しフロー）

**Files:**
- Create: `tests/e2e/specs/article.spec.ts`
- Create: `tests/e2e/specs/hero.spec.ts`

**Interfaces:**
- Consumes: helpers（`unique` / `pngPixel` / `createCategoryViaApi`）。デフォルト storageState = admin。

- [ ] **Step 1: article.spec.ts**

カテゴリ作成だけは UI（AdminCategoriesPage の検証を兼ねる）。他 spec は API helper を使う。

```ts
import { expect, test } from '@playwright/test';
import { unique } from '../helpers/data';

test('カテゴリ作成 → 記事作成 → 必須ガード → 公開 → 詳細 → フィード', async ({ page }) => {
  const category = unique('E2Eカテゴリ');
  const title = unique('E2E 記事');

  // 1) admin がカテゴリ管理 UI から作成
  await page.goto('/admin/categories');
  await page.getByLabel('名称').fill(category);
  await page.getByRole('button', { name: '追加' }).click();
  await expect(page.getByText(category)).toBeVisible();

  // 2) 新規記事: タイトル → 自動保存インジケータ
  await page.goto('/articles/new');
  await page.getByPlaceholder('タイトルを入力').fill(title);
  await expect(page.getByText(/保存しました|保存済み/)).toBeVisible({ timeout: 15_000 });

  // 3) 本文（リッチエディタ = ProseMirror）
  await page.locator('.ProseMirror').click();
  await page.keyboard.type('E2E 本文テキスト。');

  // 4) 公開パネル: カテゴリ必須ガード → 選択で解除 → 公開
  await page.getByRole('button', { name: '公開する' }).click();
  const dialog = page.getByRole('dialog');
  await expect(dialog.getByRole('button', { name: '公開する' })).toBeDisabled();
  await expect(dialog.getByText(/カテゴリの選択が必要/)).toBeVisible();
  await dialog.getByLabel(/カテゴリ/).selectOption({ label: category });
  await expect(dialog.getByRole('button', { name: '公開する' })).toBeEnabled();
  await dialog.getByRole('button', { name: '公開する' }).click();

  // 5) 記事詳細へ遷移（/articles/new に誤一致しないよう uuid 末尾アンカー）
  await expect(page).toHaveURL(/\/articles\/[0-9a-f-]{36}$/);
  await expect(page.getByRole('heading', { name: title })).toBeVisible();
  await expect(page.getByText('E2E 本文テキスト。')).toBeVisible();

  // 6) フィード掲載
  await page.goto('/');
  await expect(page.getByRole('heading', { name: title })).toBeVisible();
});
```

- [ ] **Step 2: hero.spec.ts**

```ts
import { expect, test } from '@playwright/test';
import { pngPixel, unique } from '../helpers/data';
import { createCategoryViaApi } from '../helpers/api';

test('ヒーロー画像: アップロード → プレビュー → 公開 → 詳細ヒーロー → 一覧サムネ', async ({ page }) => {
  const category = unique('E2Eヒーローカテゴリ');
  const categoryId = await createCategoryViaApi(page, category);
  const title = unique('E2E ヒーロー記事');

  await page.goto('/articles/new');
  await page.getByPlaceholder('タイトルを入力').fill(title);

  // 実 PNG をアップロード（MinIO 実往復 + magic-byte 検証を通す）
  await page.getByLabel('ヒーロー画像を選択').setInputFiles({
    name: 'hero.png',
    mimeType: 'image/png',
    buffer: pngPixel,
  });
  // エディタ内プレビュー（HeroImage の前景 img）
  await expect(page.getByRole('img', { name: 'ヒーロー画像' })).toBeVisible({ timeout: 15_000 });

  // 公開
  await page.getByRole('button', { name: '公開する' }).click();
  const dialog = page.getByRole('dialog');
  await dialog.getByLabel(/カテゴリ/).selectOption({ label: category });
  await dialog.getByRole('button', { name: '公開する' }).click();
  await expect(page).toHaveURL(/\/articles\/[0-9a-f-]{36}$/);

  // 詳細のヒーロー（HeroImage 前景 img は alt=記事タイトル）
  await expect(page.getByRole('img', { name: title })).toBeVisible();

  // フィードのカードに 4:3 サムネ（/api/uploads/ を指す img）
  await page.goto('/');
  const card = page.locator('article').filter({ hasText: title });
  await expect(card.locator('img[src^="/api/uploads/"]').first()).toBeVisible();
  const void1 = categoryId; // helper の返り値は本 spec では未使用（lint 回避のため参照）
  void void1;
});
```

（`categoryId` 未使用が気になる場合は `createCategoryViaApi` の戻り値を受けず `await` のみで良い。上記はどちらでも可。）

- [ ] **Step 3: 実行して確認**

Run: `pnpm run e2e -- specs/article.spec.ts specs/hero.spec.ts`
Expected: 2 件 PASS。Mailpit・dev スタックに影響なし。

- [ ] **Step 4: Commit**

```bash
git add tests/e2e/specs
git commit -m "feat(e2e): add article lifecycle and hero image specs"
```

---

### Task 5: search / engagement / notification spec

**Files:**
- Create: `tests/e2e/specs/search.spec.ts`
- Create: `tests/e2e/specs/engagement.spec.ts`
- Create: `tests/e2e/specs/notification.spec.ts`

**Interfaces:**
- Consumes: helpers（`unique` / `createCategoryViaApi` / `createPublishedArticleViaApi`）、member storageState（`.auth/member.json`）。

- [ ] **Step 1: search.spec.ts**

```ts
import { expect, test } from '@playwright/test';
import { unique } from '../helpers/data';
import { createCategoryViaApi, createPublishedArticleViaApi } from '../helpers/api';

test('検索: 公開記事はヒットし、下書き固有語は漏れない', async ({ page }) => {
  const categoryId = await createCategoryViaApi(page, unique('E2E検索カテゴリ'));
  const publishedWord = `pubword${Date.now()}`;
  const draftWord = `draftword${Date.now()}`;

  // 公開記事
  await createPublishedArticleViaApi(page, {
    title: unique('E2E 検索記事'),
    bodyMd: `検索用の固有語 ${publishedWord} を含む本文。`,
    categoryId,
  });
  // 下書き（公開しない）: 直接 API で作成のみ
  const draft = await page.request.post('/api/articles', {
    data: { title: unique('E2E 下書き'), bodyMd: `下書き固有語 ${draftWord}。`, categoryId, heroImageUploadId: null, tags: [] },
    headers: { origin: 'http://localhost:54173' },
  });
  expect(draft.ok()).toBeTruthy();

  // ヒットする
  await page.goto('/search');
  await page.getByLabel('キーワード').fill(publishedWord);
  await page.getByRole('button', { name: '検索' }).click();
  await expect(page.getByText(new RegExp(publishedWord))).toBeVisible();

  // 下書きは漏れない
  await page.getByLabel('キーワード').fill(draftWord);
  await page.getByRole('button', { name: '検索' }).click();
  await expect(page.getByText(/結果がありません|見つかりません|0 ?件/)).toBeVisible();
  await expect(page.getByText(new RegExp(draftWord)).first()).toHaveCount(0);
});
```

（空結果の文言は SearchPage の EmptyState 実文言に合わせて実装時に 1 度だけ調整して良い。draftWord が画面に**出ない**ことのアサーションが本体。※`toHaveCount(0)` は `first()` と併用不可のため、実装時は `await expect(page.getByText(new RegExp(draftWord))).toHaveCount(0);` とする。）

- [ ] **Step 2: engagement.spec.ts**

```ts
import { expect, test } from '@playwright/test';
import { unique } from '../helpers/data';
import { createCategoryViaApi, createPublishedArticleViaApi } from '../helpers/api';

test('コメント → 返信 → リアクション → ブックマーク → ブックマーク一覧', async ({ browser, page }) => {
  // admin が記事を用意
  const categoryId = await createCategoryViaApi(page, unique('E2Eエンゲージ'));
  const title = unique('E2E エンゲージ記事');
  const articleId = await createPublishedArticleViaApi(page, { title, bodyMd: '議論の種。', categoryId });

  // member がコメント
  const memberContext = await browser.newContext({ baseURL: 'http://localhost:54173', storageState: '.auth/member.json' });
  const member = await memberContext.newPage();
  await member.goto(`/articles/${articleId}`);
  await member.getByLabel('コメント').fill('E2E コメントです');
  await member.getByRole('button', { name: 'コメントする' }).click();
  await expect(member.getByText('E2E コメントです')).toBeVisible();

  // admin が返信
  await page.goto(`/articles/${articleId}`);
  await expect(page.getByText('E2E コメントです')).toBeVisible();
  await page.getByRole('button', { name: '返信', exact: true }).first().click();
  await page.getByLabel('コメント').last().fill('E2E 返信です');
  await page.getByRole('button', { name: '返信する' }).click();
  await expect(page.getByText('E2E 返信です')).toBeVisible();

  // member がリアクション（👍 が 0 → 1）
  const reactions = member.getByRole('group', { name: 'リアクション' });
  await reactions.getByRole('button').first().click();
  await expect(reactions.getByRole('button').first()).toHaveText(/1/);

  // member がブックマーク → 一覧に載る
  await member.getByRole('button', { name: 'ブックマーク', exact: true }).click();
  await expect(member.getByRole('button', { name: 'ブックマーク済み' })).toBeVisible();
  await member.goto('/me/bookmarks');
  await expect(member.getByRole('heading', { name: title })).toBeVisible();

  await memberContext.close();
});
```

- [ ] **Step 3: notification.spec.ts**

```ts
import { expect, test } from '@playwright/test';
import { unique } from '../helpers/data';
import { createCategoryViaApi, createPublishedArticleViaApi } from '../helpers/api';

test('member のコメントで admin に通知（バッジ → 一覧 → 既読）', async ({ browser, page }) => {
  // admin が記事を用意
  const categoryId = await createCategoryViaApi(page, unique('E2E通知'));
  const title = unique('E2E 通知記事');
  const articleId = await createPublishedArticleViaApi(page, { title, bodyMd: '通知テスト。', categoryId });

  // member がコメント（通知イベント発生）
  const memberContext = await browser.newContext({ baseURL: 'http://localhost:54173', storageState: '.auth/member.json' });
  const member = await memberContext.newPage();
  await member.goto(`/articles/${articleId}`);
  await member.getByLabel('コメント').fill('通知を飛ばすコメント');
  await member.getByRole('button', { name: 'コメントする' }).click();
  await expect(member.getByText('通知を飛ばすコメント')).toBeVisible();
  await memberContext.close();

  // admin 側: ベルに未読バッジ（ページ再訪でカウント再取得）
  await page.goto('/');
  await expect(page.getByLabel(/未読 \d+ 件/)).toBeVisible({ timeout: 15_000 });

  // 通知一覧に行が出る → クリックで記事へ（既読化）
  await page.getByRole('button', { name: '通知' }).click();
  await page.getByRole('link', { name: '通知をすべて見る' }).or(page.getByRole('link', { name: /すべて/ })).first().click().catch(async () => {
    await page.goto('/notifications');
  });
  await expect(page.getByText(title)).toBeVisible();
  await page.getByText(title).first().click();
  await expect(page).toHaveURL(new RegExp(`/articles/${articleId}`));

  // すべて既読（通知ページの操作）→ バッジ消滅
  await page.goto('/notifications');
  const markAll = page.getByRole('button', { name: /すべて既読/ });
  if (await markAll.isEnabled().catch(() => false)) await markAll.click();
  await expect(page.getByLabel(/未読 \d+ 件/)).toHaveCount(0);
});
```

（ベルのポップオーバー内リンク文言・「すべて既読」ボタンの実文言は NotificationBell.tsx / NotificationsPage.tsx を読んで実装時に正確な `getByRole` に置き換えること。**通知が出る・記事に飛べる・既読でバッジが消える**の 3 アサーションは弱めない。）

- [ ] **Step 4: 実行して確認**

Run: `pnpm run e2e -- specs/search.spec.ts specs/engagement.spec.ts specs/notification.spec.ts`
Expected: 3 件 PASS。

- [ ] **Step 5: フルスイートをローカルで一気通し**

Run: `pnpm run e2e:down && pnpm run e2e:up && pnpm run e2e`
Expected: setup 1 + chromium 7（auth2/mobile1/article1/hero1/search1/engagement1/notification1）PASS、sso は skip（Keycloak 未起動時）。クリーン DB からの一気通しが通ることが受け入れ基準。

- [ ] **Step 6: Commit**

```bash
git add tests/e2e/specs
git commit -m "feat(e2e): add search, engagement, and notification specs"
```

---

### Task 6: CI 統合 + ドキュメント

**Files:**
- Modify: `.github/workflows/ci.yml`
- Modify: `README.md`（開発コマンドに E2E を 2〜3 行追記）

**Interfaces:**
- Consumes: ルート `e2e:up` / `e2e`（Task 1）。

- [ ] **Step 1: ci.yml に並列 e2e ジョブを追加**

verify ジョブは不変。以下を jobs に追加:

```yaml
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @knowledge-hub/e2e exec playwright install chromium --with-deps
      - run: pnpm run e2e:up
      - run: pnpm run e2e
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: |
            tests/e2e/playwright-report
            tests/e2e/test-results
          retention-days: 7
```

- [ ] **Step 2: README に E2E コマンド追記**

「開発」セクション（`pnpm run verify` 等の近く）へ:

```markdown
- E2E（Playwright、専用の隔離スタックを使用）:
  `pnpm run e2e:up`（初回/リセット時） → `pnpm run e2e` → `pnpm run e2e:down`（片付け）
  SSO spec は dev Keycloak（`docker compose --profile idp up -d`）起動時のみ実行される。
```

- [ ] **Step 3: verify の非回帰確認**

Run: `pnpm run verify`
Expected: 従来どおり exit 0（E2E は混入しない。tests/e2e の typecheck が追加で走るのは可）。

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/ci.yml README.md
git commit -m "ci: add parallel e2e job running the playwright suite"
```

---

## 最終確認（全タスク後・マージ前）

- [ ] `pnpm run e2e:down && pnpm run e2e:up && pnpm run e2e` がクリーン状態から全緑
- [ ] Keycloak を起動（`docker compose --profile idp up -d`）して `pnpm run e2e -- --project=sso` が PASS（コントローラー実施）
- [ ] `pnpm run verify` exit 0・所要時間ほぼ不変
- [ ] dev スタック/dev DB に影響なし（`docker compose ps`（無印プロジェクト）と khub DB の記事数が不変）
- [ ] 最終ホールブランチレビュー → fix wave → `--no-ff` merge（tree 同一性検証）。push はユーザー判断（しない）

## Self-Review（計画 vs spec）

- **§2 決定事項**: 2 分割 → 本計画は 4c-1 のみ ✓／SSO ローカル限定 optional → sso プロジェクト + fetch probe skip + CI でも自動 skip ✓／CI 別ジョブ → Task 6 ✓／隔離スタック → Task 1（khub-e2e、!override ポート、毎回 migrate+seed、down -v）✓
- **§4.1 preview + proxy**: vite.config env 化 + webServer env で API_PROXY_TARGET=53000 ✓。フォールバック（preview proxy 不調時は明示 preview.proxy → dev サーバー）は Task 1 Step 9 のスモークで即検出できる ✓
- **§4.3 setup プロジェクト**: auth.setup.ts が admin/member の storageState を生成、招待フロー検証を兼ねる ✓。spec ファイル順序への依存なし（各 spec が自分のカテゴリ/記事を API helper で用意）✓
- **§5 スイート表**: 9 spec すべてにタスクあり（auth/mobile/sso=Task3、article/hero=Task4、search/engagement/notification=Task5、setup=Task2）✓
- **§6 CI**: Task 6 のジョブが spec §6 の 6 手順と一致（artifact 保存含む）✓
- **§7 決定論性**: workers=1／unique() データ／CI retries=1／sleep なし（mailpit ポーリングは deadline 付き helper）✓
- **型/シグネチャ整合**: helpers の export（unique/ADMIN/MEMBER/pngPixel/clearMailbox/latestMessageText/createCategoryViaApi/createPublishedArticleViaApi）は Task 2 定義 = Task 4/5 消費で一致 ✓
- **プレースホルダ**: 通知ポップオーバー内の 2 文言のみ「実装時に実文言へ置換」と明示（アサーション本体は固定）。他は実コード採取済みセレクタ ✓
- **リスク明記**: compose `!override` は v2.24.4+ 必須（Global Constraints）／`pnpm -r test` 混入禁止（e2e script 名）✓
